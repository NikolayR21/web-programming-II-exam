name: Build Vue
on: [push]
jobs:
  build_vue:
    runs-on: ubuntu-latest
    name: Build Vue
    steps:
    - uses: actions/checkout@v2
    - run: |
            cd recipes-app
    - id: Build-Vue
      uses: xRealNeon/VuePagesAction@1.0.0
      with:
        username: 'NikolayR21'
        reponame: 'web-programming-II-exam'
        token: ${{ secrets.GITHUB_TOKEN }}